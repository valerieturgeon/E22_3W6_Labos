@model int
<div class="row">
    <div class="col">
      <button type="submit" class="btn btn-warning form-control" asp-route-id="@Model">@SharedLocals["Update"]</button>
</div>
    <div class="col">
      <a asp-action="Index" class="btn btn-success form-control">@SharedLocals["Back to List"]</a>
</div>
</div>